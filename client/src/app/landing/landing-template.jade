.container
  .row.text-center
    h1 The project that allows us to keep our Congress accountible.  We the people.
      
  .row.text-center
    span Find
    br
    form(name="form.landingPage")
      .row
        .col-md-3.col-sm-4.col-md-offset-3.col-sm-offset-2
          .input-group
            input.form-control(type="text", placeholder="Bills", ng-click="findTopic", ng-model="searchTopic")
            span.input-group-btn
              button.btn.btn-default(type="button", ng-click="findTopic()") Go
            
        .col-md-3.col-sm-4
          .input-group
            input.form-control(type="text", placeholder="District", ng-model="zipCode")
            .input-group-btn
              hu-geolocate
              button.btn.btn-default(type="button", ng-click="findDistrictLanding()")
                span Go
            
    br
    span We never store or share your info with third parties, let alone persist it.